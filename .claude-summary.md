# VoxFlame Agent - 当前项目摘要

> 最后更新：2026-09-11
>
> 这里只保留启动会话必须知道的现役事实、近期决定和未完成边界。历史以 Git 和 `research/` 中的专项事实源为准。

## 文档与协作

- `research/` 是仓库唯一文档根，承接研究、产品、运行时、工程、运维、合规和模板；不再恢复平级 `docs/`。
- 系统规则、Harness、执行模板分别位于 `research/AI_ENGINEERING_SYSTEM.md`、`research/aiprompts/`、`research/templates/`。
- 采集四项需求、LiveKit/阿里云扩容、Backend 控制面和质检边界统一见 `research/product-engineering/VOICE_COLLECTION_SCALING_CONTROL_PLANE_2026-09-04.md`。
- 完成任务后同步本文件与 `.tasks/current.md`；历史故障、压测流水和已完成旧计划不在入口文件持续累积。

## 产品与运行时

- 第一原则：提升系统对构音障碍用户意图的理解，而不是纠正用户的声音。优先级为 `可理解性 > 低延迟 > 可打断 > UI 精修`。
- 唯一主链：`Frontend LiveKit RTC/Data -> Backend /api/rtc/session/* -> self-hosted livekit-server -> livekit_agent`；不恢复 WebSocket、TEN 或 Agora 平行主链。
- Web 的 `/communicate`、`/practice`、`/assessment`、`/contribute` 已按沟通、筛查和数据录入分工；Mobile Workbench 是独立原生 sibling client，与 Web 复用 Backend contract，不依赖 Next.js 运行时。
- Web 与 Mobile 的注册/登录已统一为一个用户可见授权勾选框，并在认证元数据中继续分别记录隐私、敏感信息、数据采集和商业用途四项审计字段；授权版本为 `2026-09-06`。四份法律文件保留产品名“生声不息”；2026-09-10 按用户指定，将隐私政策与用户服务协议中的运营主体、个人信息处理者及联系主体名称改为“杭州燃言科技”。另两份文件未单列旧主体；第三方服务商与 ICP 备案信息保持不变。相关改动尚未部署，正式应用市场提交前仍需律师复核、Supabase 实际区域/跨境安排确认和真机协议链接验收。
- durable memory 的 owner 是 `backend + workspace snapshot`；LiveKit 只承接 session-local state 和会话原始材料。
- 训练录音上传已增加服务端授权、OSS 对象和 metadata 准入；训练导出采用不可覆盖快照、manifest/receipt 对账、质量 hard gate 和 speaker-disjoint split。相关改动尚未部署，仍需生产 backfill dry-run 与真实 Web/Mobile 上传 smoke。
- Web 训练录音停止已与最终 ASR/上传解耦：本地 WAV 收尾后立即释放操作，最终转写按 `client_capture_id` 后台补齐并保存；RTC 停止消息有 400ms 单操作超时，浏览器 `AudioContext.close()` 不再阻塞。该修复已部署到主前端，容器 healthy、RTC 健康口正常；仍需真实账号连续多句验收。
- 方言模式当前完成研究登记和 metadata 传递；用户自报方言只作来源标签，普通话/方言用 `utterance_pair_id` 配对。训练页双录状态机、迁移和真机验证仍未完成。
- Web/App 方言注册已实现多条“名称＋来源地区”，省市明确为现居地；录音固定本次选择，质检不再使用个人档案方言兜底。2026-09-07 14:52 只读核验确认 Web/Backend 已部署且公网表单可用，但生产缺 `dialect_profiles` 列、仍为旧触发器，人工质检审计表也未创建；须先验证并定向迁移，再验收真实双录/上传与真机；见 [实现与发布边界](research/product-engineering/DIALECT_PROFILE_IMPLEMENTATION_2026-09-07.md)。

## 实时并发与 ASR

- 2026-09-10：固有闭环“发现需求 → 实现 → 优化 → 测试 → 迭代”已纳入规则/模板/入口与研究检查。[语音官方工程指标协议](research/voice-agent/VOICE_AGENT_BENCHMARK_PROTOCOL.md)区分 LiveKit/W3C 定义与项目 SLO，离线 scorer/CER/缺测守卫沿 RO-014/FB-008 回流；现有 RTC 探针修正计时/百分位并要求显式真实调用授权。121项Agent与22项Harness测试通过，32次本地TTS取消微测仅为内存替身机制证据；真实ASR准确率、RTC/设备延迟、AEC未测，不部署。当时治理CI缺脚本；2026-09-11已补本地治理入口，远端CI仍待验证。

- 2026-09-10 [现役语音链路诊断](research/product-engineering/LIVE_VOICE_PIPELINE_DIAGNOSTIC_2026-09-10.md)：线上为服务端 NS/高通（AEC 关闭）→ RMS VAD → 分句 HTTP ASR → `qwen-flash` 整句纠错 → 按模式启用 `qwen3-tts-flash-realtime`；Web 沟通/训练工作台默认关闭 Agent 自动 TTS。隔离复现确认 TTS 打断未清 Python 待播帧、可回灌旧音频；输入消费等待 ASR、回复 FIFO 亦有积压风险。[本地修复](research/voice-agent/RO-014-voice-pipeline-repair-2026-09-10.md)已移除旧TTS队列/回复FIFO/同步LLM，HTTP ASR分离收音与识别、按capture隔离，增加半双工保护及退出回收；118项Agent测试通过。未部署；硬件AEC/尾音和真实RTC仍待验收，不宣称识别准确率已提升。

- LiveKit TTS 仍走 DashScope realtime 主链；新增 `DASHSCOPE_TTS_MODEL` / `ALIYUN_TTS_MODEL` 模型覆盖配置，默认保持 `qwen3-tts-flash-realtime`，可在同一协议兼容的阿里云模型间切换。代码和单测已完成，具体模型需真实账号音频 smoke 后再作为默认。

- LiveKit Agents 已升级到 `1.7.1`，LiveKit Server 已升级到 `1.13.6`；每周工作流只提醒评估，不自动升级生产。
- 当前 4 vCPU / 7.3 GiB 主机上的单 Agent Worker 已完成 8 路完整 RTC 压测：`8/8 ASR + 8/8 LLM + 8/8 TTS` 可完成；TTS 首包 P95 仍高于 1.5 秒目标。
- 生产保护值为 8 active jobs、个性化 HTTP ASR 4 槽、realtime fallback 4 槽、LLM 8 槽、TTS 3 槽。16 路需要至少第二个 8 路 Worker、GPU ASR P95 优化、TTS 配额和跨 Worker 集中配额。
- cpu1 通过 GPU1 主动建立的 SSH 反向隧道访问 `127.0.0.1:8001`。2026-09-10 只读核验监听由 `sshd` 持有，`/health` 正常；`/accounts` 显示 `3083029019` 当前默认已为 `exp39-3083029019-r392-step500-beam5-rank0`，EXP-25 仍在可用列表，不能继续把 EXP-25 当现役默认。本次仅核验注册表，未重新发送录音推理。其他两账户的历史路由记录为 `2187054680 -> EXP-29`、`2307294809 -> EXP-24`；公共 fallback 为 `exp16l3-lambda025`。旧 `8000/18000` 无监听。
- 高可用和 1,000 路尚未完成。扩容顺序是先完成 Backend 持久事件与多实例安全，再上双 Agent Worker，最后根据证据推进 LiveKit 多节点；购买资源、Provider 扩额、DNS/生产流量和旧资源释放需用户确认。

## 数据采集与研究

- 采集支持 Web/App 共用授权契约、账户隔离、本机待同步队列、严格替换重录和人工质量诊断接口；原始录音不会因自动低质量判断直接删除。
- 普通话录音证据门以 `research/speech-health/MANDARIN_RECORDING_CORPUS_EVIDENCE_GATE.md` 和覆盖基线为准；录音可见不等于训练导入。
- 81 篇鲁迅完整作品已完成来源、权利与互校门禁并进入本地题库；旧概况/提纲模板已退出运行时。该题库尚未部署。
- 研究结论必须进入 `research/APPLICATION_FEEDBACK_REGISTRY.md`；状态低于部署门槛时只能用于研究或隔离验证。

## OSS 账户映射（2026-09-08，后台同步已部署）

- [账户映射同步](research/product-engineering/OSS_ACCOUNT_IDENTITY_SYNC_2026-09-08.md)已独立发布：全体 Auth 用户自动生成私有 `dataset/<UUID>/account.json`，邮箱/手机分别保存值与验证状态，不将 PII 写入训练清单；不依赖上传录音，新用户下一轮自动补齐，联系方式变更同步、Auth 缺失清空联系人。
- 首次回填 57 个注册用户和 1 个历史 owner；13:34 独立回读确认 57/57 内容与 Auth 一致、全部 private，含 46 邮箱/11 手机及 42 个无录音用户，匿名 GET 返回 403；自动后续轮次 58 项 unchanged。11 项身份/sidecar 测试与 Backend 构建通过，systemd 每轮结束 60 秒后重试。定时同步独立于正在运行的 Backend/Web，未部署待迁移的计时代码；GPU1 仍需读取 account.json，未重建/更名现有 Mix 或 `oss-xxx`。

## 长期录音计时（2026-09-08，未部署）

- **2026-09-11 14:40 只读复核更新**：运行中 Backend（容器创建于当日 11:40）已包含 `durable_v1` 校验和上传确认账本守卫，不再符合下文 09-08 的“Backend 未部署”历史状态。使用该容器现有数据库连接只读 GET：两张计时表均返回 `404/PGRST205`，`get_recording_progress` 抽查返回历史正时长但无 `durationAccounting` 标记。确定当前应用/数据库接口契约不匹配，会拒绝进度读取与新上传确认；表未创建还是 schema cache 问题尚需定向核验，不可直接执行迁移。单纯更换 OSS/密钥不改数据库时长；迁移必须保留 Supabase、用户 UUID 和贡献记录，禁止先清理明细。本轮未更换密钥、迁移、部署或写生产数据，未做全账号基线对账。另观察到 livekit-agent 重启循环，原因未诊断。

- 已实现[独立计时账本与账号累计字段](research/product-engineering/DURABLE_RECORDING_DURATION_2026-09-08.md)：同账号/recording ID 幂等，receipt 确认与入账同事务；OSS 下载/迁移、语料清理和单条撤回不扣累计，Auth 账号注销清除统计。历史仅回填现存可核验 DB 行，不恢复未经证实的 19 小时。
- Web 主统计只显示云端确认时长，本机待确认上传单列条数；可见页面 30 秒/返回窗口/联网刷新；关闭可删除明细求和 fallback。原生上传共用 Backend 自动记入同一账本，但现有原生页未新增累计卡。
- 隔离 PostgreSQL 16 回填/确认/清理/重试/新采集/回滚/权限/时区/注销与并发测试通过；Backend 上传套件与构建、Web 150 项测试/类型/生产构建通过。Playwright 在独立本机生产构建与 mock API 上验证 `/contribute`：两浏览器总时长一致、本机两小时待传不加总、清队列不回落、返回刷新、390px 无横向溢出、503 显示暂未更新；正常路径无 console 错误，注入 503 出现预期错误。截图在本地忽略的 `output/playwright/recording-duration-mobile.png`。
- 生产 migration history 漂移未解决，定向迁移 `20260908010000`、Backend/Web 发布、原生真机及真实账号双设备验收均待确认；禁止直接 `db push --include-all`。本轮未部署、未推送 Git。

## 本次代码归档验证（2026-09-08）

- PR #19 冲突已按等价发布快照核对：分支祖先 `5ca8012` 与 main 的 `59b64c5` 文件树相同；main 后续仅 `fbd683f` 录音确认修复。保留分支后续实现和 `research/` 单文档根，补齐 main 的确认项锁定与布局守卫，不恢复旧 docs/PWA。合并前备份为 `backup/mobile-parity-before-main-20260908`；本轮前端 148 项测试/类型/生产构建、Backend 测试/构建、Mobile 全部已有回归/类型/Android+iOS export、文档/研究与 Compose 检查通过，不部署。原有 CI 缺脚本问题见当前任务。

- `.playwright-mcp/` 全目录忽略并取消旧快照的 Git 跟踪，本地文件保留；需要长期留存的验收证据另行归档到 `research/`。
- 当前分支待提交改动已通过前端 148 项测试、类型检查与生产构建、Backend 上传测试与构建、Mobile check/typecheck/训练测试、文档与 Research Harness、Compose 配置检查；20 份研究来源快照哈希一致。此次仅 Git 归档，不执行部署、数据库迁移或采购；真实账号与真机验收仍按当前任务推进。

## 最近交付：政策汇报

- 已按合伙人阅读场景完成8页《上海、杭州算力政策及业务布局建议》PDF，包含区域比较、子公司分工、投入条件、实施安排及9项官方依据；前版仅作背景记录。12份官方网页及限制见 [RO-017](research/product-engineering/RO-017-compute-policy-briefing-2026-09-07.md)。未确认公司资格或完整余杭修订细则，不计入确定补贴收入；不涉及运行时、采购和申报。

- 已交付[阿里云第一期充值审批与GPU商务规划](research/product-engineering/RO-018-aliyun-budget-and-compute-2026-09-07.md)两份中文Word/PDF。10万元充值、12万元上限仅为待批申请，GPU与50TB训练存储另行报价；补贴按零抵减，上传峰值不作RTC承诺。独立财务审核、实际报价和资格确认仍待完成，未充值或采购。

## ESP32 官方 SDK 联调（2026-09-09）

- 已复用正在运行的 Backend `LiveKitSessionService` 签发单房间、单设备、8 小时临时凭证，含现役 Agent 调度；不绑定真实账户/个性化资料，不含管理权限。凭证仅存仓库外受限交付文件，不写入文档或 Git；北京时间 2026-09-09 19:51:50 到期，后续需重新签发。
- 公网 TLS/RTC 探针连接及 `session_init_ack` 已通过，探针已断开；未改部署。ESP32 真机收音、ASR/TTS、打断和重连尚待同事联调，不代表产品方案已切换为独立联网硬件。

## RTC 接口收口（2026-09-11，本地未部署）

- Backend `src/contracts/rtc-session.ts` 是唯一可编辑 RTC HTTP wire contract；`scripts/sync-rtc-contract.mjs` 生成 Web/Mobile 副本并可用 `--check` 检测漂移。
- 删除空转 `/api/rtc/session/ping`、`/api/rtc/session/stop`、`/api/rtc/graphs`；LiveKit SDK room 是连接/断开事实源。start 响应仅保留 transport、intent、readiness 与明确命名的 JWT `joinTokenTtlSeconds`。
- Web/Mobile 客户端在打开房间前校验响应；服务端 `scene` nullability 已统一。Backend构建/HTTP回归、Web153项测试/类型/生产构建、跨端10项、Agent12项、Mobile类型/守卫/训练与双平台export通过；隔离Playwright验证无HTTP保活、主动断开/重连/异常响应，并修复旧room回调覆盖状态。未部署、未做真实设备 smoke。新旧契约不可直接混用，发布必须协调 Backend/Web/App 切换与旧版阻断。
- 第二切片：严格 `{ intent }` 请求边界、禁止客户端指定账户/房间/TTL；Web取消贯穿HTTP/SDK/ACK/profile/重试，旧任务不能覆盖新会话或播放晚到音轨；Mobile取消pending HTTP并重新签发连接凭证。跨端13项+生命周期8项、Web154项/类型/构建、Backend、Mobile双平台export、Agent12项和文档检查通过；隔离Playwright通过取消/晚到/重连且31秒无HTTP保活。未部署；原生AudioSession并发、真实麦克风/设备/AEC/CER/时延仍未验收。
- 第三切片：Native owner取消/账号切换/卸载、共享AudioSession租约与串行启停完成本地验证；16项原生替身、6项真机证据门、9项治理负例及Mobile双平台export通过。CLI/Harness入口补齐，CI显式checkout-only不要求私有凭证，默认完整检查保持严格。未部署/推送；真机AEC/CER/时延仍缺测，详见同一记录。
- 详细边界与回退见 [RTC_CONTRACT_CLEANUP_2026-09-11](research/product-engineering/RTC_CONTRACT_CLEANUP_2026-09-11.md)。

## 当前需要用户配合

注册协议已另导出四份 PDF 与四份 Word，完成正文及表格文字核对；交付包位于 cpu1 的 `/home/ubuntu/registration-agreements-pdf-word-20260910.zip`，待下载，不代表已部署或已写入 Windows 桌面。

1. Android/iPhone 真机完成登录、RTC、训练录音、上传/重试/撤回和断网恢复验收；iOS 先完成签名与设备授权。
2. 用真实账号完成 Web 沟通和 20 词整组筛查 smoke。
3. Web 数据录入页部署后连续录制至少 10 句，确认停止后可立即进入下一句、后台识别不串句且无需换浏览器。
4. 决定并购买下一阶段扩容资源、申请 TTS/LLM 配额；正式 DNS、流量迁移和旧资源释放仍需人工确认。
5. 补充第二品牌的域名、中文站名、Logo 和主色；第二品牌不得出现“燃言”信息。
6. 完成短信注册/再次登录的真实手机号和 Android 验证。
7. 正式上架前由中国大陆隐私合规律师复核统一授权方式和四份法律文件，并确认 Supabase 实际部署区域、个人信息出境路径及账号注销闭环。

## 权威入口

- 文档导航：[research/README.md](research/README.md)
- 产品边界：[research/product-engineering/VOXFLAME_PRODUCT_PRD_2026-03-24.md](research/product-engineering/VOXFLAME_PRODUCT_PRD_2026-03-24.md)
- 采集与扩容：[research/product-engineering/VOICE_COLLECTION_SCALING_CONTROL_PLANE_2026-09-04.md](research/product-engineering/VOICE_COLLECTION_SCALING_CONTROL_PLANE_2026-09-04.md)
- 工程规则：[research/AI_ENGINEERING_SYSTEM.md](research/AI_ENGINEERING_SYSTEM.md)
- 当前任务：[.tasks/current.md](.tasks/current.md)
- Research Harness：[research/RESEARCH_HARNESS.md](research/RESEARCH_HARNESS.md)

## 提交归档（2026-09-09）

- 简洁提交说明与推送回复规则已纳入 Harness，并同步三个规则入口。
- 本轮前端测试（150 项另加 4 项 manifest）、类型/生产构建、Backend 身份/上传测试与构建、Agent 配置测试、隔离数据库并发计时、文档与研究检查通过。真实设备和生产验收未复跑；已有治理 CI 缺脚本问题仍待处理。
- 本轮范围为现有修改的 Git 提交与推送；不部署、不执行生产迁移。

## 录音上传构建修复（2026-09-12）

- 修正 Web `useVoiceUpload.ts` 的 `/upload/complete` 调用：先闭合 JSON 序列化，再将认证重试参数传给请求函数。此前“类型检查通过/两端完整修复”的回复不成立；本轮新增语法及调用参数 AST 回归，旧错误变体产生13项语法诊断，新版本通过156项前端测试和 TypeScript 检查。Docker 构建验证结果见当前任务；不代表生产录音链路或 App 已验收。
- App 的401分支仍会被随后返回条件截断；Web认证强制刷新、原始录音先落盘、后台补传与308凌晨恢复也须分别验收，不得以本次编译修复宣称闭环完成。

## 308硬件调试凭证（2026-09-11）

- 按用户明确授权，复用运行中 Backend `LiveKitSessionService` 为308模型签发独立房间、独立测试身份、8小时入房Token；使用管理员授权调试标记，不读取308真实用户UUID、个人档案、memory或workspace，不授予应用能力或房间管理权限。凭证只存仓库外权限600交付包 `/home/ubuntu/livekit-308-debug-20260911.zip`，包含配置与中文说明；不得将JWT/Secret回填仓库。
- 入房有效期截至北京时间2026-09-12 01:16:19；到期不代表在线会话强制断开，测试后需主动退出。该凭证不是物理设备绑定，不能公开分享；删除文件不等于撤销。
- JWT签名/房间/dispatch/权限断言、运行中Agent离线308路由与空准备上下文检查、ZIP完整性检查通过；只读注册表确认当前默认EXP-39（`exp39-3083029019-r392-step500-beam5-rank0`），Token绑定账户而非固定模型版本。未入房、未推理、未改部署；硬件及真实模型命中待验收。
