# RO-014 跨模态实时 Agent 容量对照

通用 Agent 的并发通常由请求队列、模型 token、工具调用和状态存储决定；实时语音还必须处理连续音频、端点检测、可打断、重叠说话、网络抖动和长连接供应商配额。故本项目只把文本 Agent 的“限流、超时、熔断、显式状态”作为机制参考，不把文本 benchmark 当作语音部署证据。

LiveKit 的 Agent Job 隔离和自定义负载函数可作为运行时控制；W3C WebRTC 与本地生产指标用于媒体和设备边界。最终采用门槛仍是语音场景：陌生人沟通成功率、端到端延迟、可打断性、错误恢复、隐私和跨用户隔离。
